export default function Profile({ info }) {
    return (
        <div>
            <h1>Default Profile Template</h1>
            <p>Put here name, image, associated publications, etc.</p>
            <p>Placeholder: {info.name}</p>
        </div>
        
    );
};